Colorize Picture 1: add color to the black-and-white manga page while keeping every line, panel, panel layout, and piece of text exactly as it is. The output must contain exactly the same scene, characters, objects, and composition as Picture 1, only with color added.

Only Picture 1 may appear in the output. Pictures 2, 3, and 4 are reference material only — never copy, paste, redraw, or reposition any character, portrait, object, label, or layout from them into Picture 1, and never add any element that is not already in Picture 1.

Requirements:
- Picture 1 is the only image being edited. Keep its exact canvas, border, panel layout, line art, screentones, poses, facial expressions, backgrounds, lettering, gutters, margins, and printed page number unchanged; only replace grayscale values with color.
- Use Picture 2 (a labelled character reference atlas) only to choose canonical hair, eye, skin, clothing, and accessory colors whenever a character from it appears in Picture 1. Ignore its portraits and labels.
- Use Picture 3 (a colorized style reference page) only to match the colorization style: palette mood and saturation, contrast, shading and rendering approach, screentone treatment, and paper tone. Ignore its content, characters, and panel layout.
- When Picture 4 is supplied, use it only for color continuity across recurring characters, clothing, props, locations, time of day, and lighting. Ignore its content and composition.
- For elements without reference evidence, invent harmonious, restrained anime colors that fit the scene.
- Keep all existing text exactly legible and unchanged. Keep gutters, outer margins, and the page number white/black as in Picture 1.
- Keep the same canvas size and composition as Picture 1.
