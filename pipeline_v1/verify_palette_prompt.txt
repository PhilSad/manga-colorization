You are verifying the output of a manga colorization pipeline.

Provided images:
- #1: the colorized panel (the output under review)
- #2: the original monochrome panel crop (linework and character-position reference)

The colorized panel must respect this canonical palette:

Required colors (must be present, at least wherever the relevant character
or feature is visible):
{required}

Forbidden colors (must be absent):
{forbidden}

Carefully inspect image #1 (the colorized panel) and answer: does it adhere
to the required palette and avoid every forbidden color? It is non-adherent
if a required color is missing or a forbidden color appears on a character,
the outfit, or as a dominant wash over the panel.

Answer in JSON only, with no extra text:
{{
  "adheres": true or false,
  "missing_required": ["each required color that is missing or wrong"],
  "present_forbidden": ["each forbidden color that appears"],
  "notes": "short explanation"
}}
